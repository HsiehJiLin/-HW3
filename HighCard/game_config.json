{
  "server": {
    "command": [
      "{python}",
      "server/server.py",
      "{matchId}",
      "{roomId}",
      "{startAt}",
      "{user0}",
      "{user1}"
    ]
  },
  "client": {
    "command": [
      "{python}",
      "client/client.py",
      "{host}",
      "{port}",
      "{userId}",
      "{linuxuser}"
    ]
  }
}
